# Core
google-generativeai>=0.7.0
google-genai>=0.7.0
python-dotenv>=1.0.0

# HTTP (used internally by google-generativeai, pinned for stability)
httpx>=0.27.0

# Email
# smtplib is part of stdlib — no extra install needed

# Testing
pytest>=8.0.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0

# Type checking (optional but recommended)
mypy>=1.10.0